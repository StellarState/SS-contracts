[workspace]
resolver = "2"
members = [
    "contracts/invoice-escrow",
    "contracts/invoice-token",
    "contracts/payment-distributor",
]

[workspace.dependencies]
soroban-sdk = "22.0.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
